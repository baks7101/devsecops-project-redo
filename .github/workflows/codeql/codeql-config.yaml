name: "Python CodeQL config"

paths:
  - .    # <- explicitly include repo root

# Optional ignores – you can tweak these later
paths-ignore:
  - '**/tests/**'
  - '**/*.md'
  - '.github/workflows/**'
